<?xml version="1.0" encoding="UTF-8"?>
<!-- You may freely edit this file. See harness/README in the NetBeans platform -->
<!-- for some information on what you could do (e.g. targets to override). -->
<!-- If you delete this file and reopen the project it will be recreated. -->
<project name="00_CONTRACT" basedir=".">
    <description>Builds the module suite 00_CONTRACT.</description>
    <import file="nbproject/build-impl.xml"/>
    <!-- Modifies the default settings for JVM (ANTES DEL CIERRE DE la etiqueta project)-->
    <target name="build-launchers" depends="suite.build-launchers">
    <replace file="build/launcher/etc/${app.name}.conf" token="--branding 00_contract -J-Xms24m -J-Xmx64m" value="--branding 00_contract -J-Xms128m -J-Xmx512m"/>
    </target>
    <!-- Modifies the default settings for JVM -->
</project>
